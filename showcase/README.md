# showcase — نَسَق

| Extension | Use |
|-----------|-----|
| `.nq` | Source + compiled module |
| `.nqr` | Runtime (core, dom, router) |
| `nasaq.toml` | Project manifest |

```bash
nasaq check .
nasaq build .   # → dist/showcase.nq
nasaq dev .
```

Template: `web`
